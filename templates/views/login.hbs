<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <style>
        body {
            margin: 0;
            padding: 0;
            font-family: Arial, sans-serif;
            {{!-- display: flex;
            justify-content: center;
            align-items: center; --}}
            min-height: 100vh;
           
            
        }

        .container {
            max-width: 400px;
            margin: 50px auto;
            padding: 20px;
            background-color: #ffffff40;
            border-radius: 5px;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
        }

        h1 {
            text-align: center;
            margin-bottom: 20px;
        }

        form {
            display: flex;
            flex-direction: column;
        }

        label {
            margin-bottom: 5px;
        }

        input {
            padding: 8px;
            margin-bottom: 10px;
            border: 1px solid #ccc;
            border-radius: 3px;
            box-sizing: border-box;
            width: 100%;
        }

        .button1 {
            padding: 10px;
            margin-top: 10px;
            background-color: #007bff;
            color: #fff;
            border: none;
            border-radius: 3px;
            cursor: pointer;
            width: 100%;
        }

        .button1:hover {
            background-color: #0056b3;
        }

        p {
            text-align: center;
            margin-top: 10px;
        }

        a {
            color: #007bff;
            text-decoration: none;
        }

        a:hover {
            text-decoration: underline;
        }

        .forgot-password {
            text-align: center;
            margin-top: 10px;
        }

        .forgot-password a {
            color: #007bff;
            text-decoration: none;
        }

        .forgot-password a:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>
    {{>navbar}}
    <div class="container">
        <h1>Login</h1>
        <form action="/login" method="POST">
            <label for="email">Email:</label>
            <input type="email" id="email" name="email"  placeholder="Email">
            <label for="password">Password:</label>
            <input type="password" id="password" name="password"  placeholder="password">
              {{#if errorMessage}}
    <div id="errorMessage" style="color: rgb(161, 24, 24);">{{errorMessage}}</div>
    <script>
        setTimeout(() => {
            var errorMessageElement = document.getElementById('errorMessage');
                 errorMessageElement.style.display = 'block';
            if (errorMessageElement) {
                errorMessageElement.style.display = 'none';
            }
        }, 2000); // Hide the error message after 2 seconds


        
    </script>
{{/if}}
            <button type="submit" class="button1">Login</button>
        </form>
        <p class="forgot-password">Forgot your password? <a href="/forgot-password" style="color:blue;">Reset it</a></p>
        <p>Don't have an account? <a href="/register" style="color: blue;">Register</a></p>
    </div>
</body>
</html>

